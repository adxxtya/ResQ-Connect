/*
This query looks for nodes, ways, and relations with the given key/value combination in India.
*/
[out:json][timeout:100];

// Define a GeoJSON representation of India's boundary
{{geocodeArea:India}};

// gather results
(
  // query part for: “amenity=police”
  node["amenity"="police"](area);
  way["amenity"="police"](area);
  relation["amenity"="police"](area);
);

// print results
out body;
>;
out skel qt;
